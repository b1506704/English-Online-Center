export const isUser = (loginInfo)  =>  loginInfo.isUser === true;

export const isCoacher = (loginInfo)  =>  loginInfo.isCoacher === true;

export const isAcademicManager = (loginInfo) => loginInfo.isAcademicManager === true;

export const isHRManager = (loginInfo) =>  loginInfo.isHRManager === true;

export const isCenterManager = (loginInfo) =>  loginInfo.isCenterManager === true;

export const isSalesManager = (loginInfo)  => loginInfo.isSalesManager === true;

